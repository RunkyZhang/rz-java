package com.vf.common.architecture.log;

public class DefaultAccessLogStrategy extends AbstractAccessLogStrategy {
    public DefaultAccessLogStrategy(AccessLogPolicy accessLogPolicy) {
        super(accessLogPolicy);
    }
}
